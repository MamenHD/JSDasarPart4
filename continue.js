// Penggunaan Continue di JavaScript

for (let i = 0; i < 10; i++) {
    if (i === 5) {
        continue;
    }
    console.log(i); //Output akan mengabaikan kondisi if dan melanjutkan sampai kondisi for
  }
